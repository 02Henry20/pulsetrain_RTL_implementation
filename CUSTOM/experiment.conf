# Global experiment defaults read by CUSTOM/run_all.sh.
# Per-input paths, dimensions, MAX_BL values, pulse sources, and optional
# pulse-time overrides live in inputs.json. Architecture/input job selection and
# per-stage switches live in architectures.json.
INPUTS_JSON="inputs.json"
ARCHITECTURES_JSON="architectures.json"
STOCHASTIC_VALUE_WIDTH=16
OUTPUT_BUFFER_DEPTH=6
DIGITAL_CLOCK_NS=10
DEFAULT_PULSE_TIMES_NS=(10 100 1000)
# Use unsigned decimal here; VCS re-evaluates -pvalue arguments.
LFSR_SEED=44257
SYNTH_TARGET_PERIOD_NS=3
